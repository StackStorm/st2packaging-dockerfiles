export BASE_DISTRO="ubuntu:xenial"

export UPDATE_AND_UPGRADE="apt -y update && apt -y upgrade"
export CLEAN_PACKAGES="apt clean"
export INSTALL_PACKAGE="apt -y install"
export REMOVE_PACKAGE="apt -y remove"
